[project]
name = "tmuxbot"
version = "0.1.0"
description = "Telegram <-> tmux Claude Code TUI bridge (单文件极简版)"
requires-python = ">=3.10"
dependencies = [
    "aiogram>=3.13",
    "pyyaml>=6.0",
    "python-dotenv>=1.0",
]

[project.optional-dependencies]
feishu = [
    "lark-oapi>=1.4",
]

[project.authors]
name = "ztllll"
email = "33171642+ztllll@users.noreply.github.com"
