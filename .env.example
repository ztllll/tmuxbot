# claude bot (默认 backend = claude_code)
TG_BOT_TOKEN=xxx:yyy

# codex bot (可选, 若启用 codex backend 才需要)
# TG_CODEX_BOT_TOKEN=xxx:yyy

# 留 0 = setup mode: bot 收到第一条 DM 自动锁定发送者 user_id 并写回此文件
BOSS_USER_ID=0
LOG_LEVEL=INFO
